import { AnnotationMoveDirective } from './annotation-move.directive';

describe('AnnotationMoveDirective', () => {
  it('should create an instance', () => {
    const directive = new AnnotationMoveDirective();
    expect(directive).toBeTruthy();
  });
});
